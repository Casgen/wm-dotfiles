# My i3 Windows Manager (or other) dotfiles

- `i3config.conf` - config file for i3
- `config.polybar.ini` - config file for polybar
- `picom.conf` - config file for picom
- `tmux.conf` - tmux config file
